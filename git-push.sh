# Uncomment these if git commit cribs saying email and name are needed
# git config user.email "you@example.com"
# git config user.name "Your Name"
git add .
git commit -m "updates"
git push origin master
